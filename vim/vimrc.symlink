syntax on

" Show line numbers
set number

" Highlight cursor line
set cursorline

" Enable smart case (insensitive searches unless query includes capital
" letters - then it will automatically switch to case sensitive. Handy.)
set ignorecase
set smartcase

" Wrap gitcommit file types at the appropriate length
filetype indent plugin on
